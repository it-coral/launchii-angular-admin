machine:
  timezone:
    America/Los_Angeles

dependencies:
  pre:
    - npm install -g karma-cli bower
    - bower install
  cache_directories:
    - ~/nvm

deployment:
  production:
    branch: master
    heroku:
        appname: launchii-admin

